import { CustomCQPipePip} from './custom-CQ-pipe.pipe';

describe('CustomCQPipePip', () => {
  it('create an instance', () => {
    const pipe = new CustomCQPipePip();
    expect(pipe).toBeTruthy();
  });
});
